* undo/redo & 

* deferreds

* module.ready = module.ready + states.ready

* default view 
** (default options wegmachen und dann sieht man es) : keine beschriftung
** getTransitions( 'toolbar') wird nicht angezeigt
** default template wird nicht gerendert

* state/view setzen haut noch nicht richtig hin

* promise in $promise umbenennen 

* ampere in $ampere umbenennen  

* render engine
** http://www.whatwg.org/specs/web-apps/current-work/multipage/timers.html#dom-showmodaldialog
** https://developer.mozilla.org/En/DOM:window.showModalDialog