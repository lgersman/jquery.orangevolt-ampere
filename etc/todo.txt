* automatic transition support (including undo/redo integration)

* CRUD test app (either via "states imported from module" or "module wrapped as state")
* generic back/cancel button (-> shortcut for undo)
